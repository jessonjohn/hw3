<h1><%= @place.name %></h1>
      
<h2>Posts</h2>
<ul>
    <% for post in @place.posts %>
        <h3> <%= post.title %> </h3>
        <%= post.posted_on %>
        <p> <%= post.description %> </p>
            
        </li>
    <% end %>
</ul>

<a href="/posts/new?place_id=<%= @place.id %>">Add a Post</a>

<a href="/places">Back to all places</a>